\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{subcaption}
\usepackage{tabularx}
\usepackage{booktabs}
% \usepackage{float}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{textcomp}
\usepackage{xurl}
\usepackage{array}
\renewcommand{\arraystretch}{1.2}
\usepackage{listings}
\lstdefinestyle{ieeelisting}{
	basicstyle=\ttfamily\footnotesize,
    frame=single,
    breaklines=true,
    numbers=none,
    showstringspaces=false,
    columns=fullflexible
	}
\usepackage{acronym}
\newacro{ai}[AI]{Artificial Intelligence}
\newacro{ann}[ANN]{Artificial Neural Networks}
\newacro{dl}[DL]{Deep Learning}
\newacro{ml}[ML]{Machine Learning}
\newacro{cv}[CV]{Computer Vision}
\newacro{cnn}[CNN]{Convolutional Neural Network}
\newacro{dcnn}[DCNN]{Deep Convolutional Neural Network}
\newacro{svm}[SVM]{Support Vector Machine}
\newacro{se}[SE]{Squeeze-and-Excitation}
\newacro{map}[mAP]{Mean Average Precision}
\newacro{vit}[ViT]{Vision Transormer}
\newacro{yolo}[YOLO]{You Look Only Once}
\newacro{fpn}[FPN]{Feature Pyramid Network}
\newacro{iou}[IoU]{Intersection over Union}
\newacro{gradcam}[Grad-CAM]{Gradient-weighted Class Activation Mapping}

\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\begin{document}

\title{PierceStageNet: Explainable EfficientNet for Grapevine Pierce’s Disease Stage Classification}

\author{\IEEEauthorblockN{1\textsuperscript{st} Sehrish Noreen}
	\IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
		\textit{name of organization (of Aff.)}\\
		City, Country \\
		email address or ORCID}
	\and
	\IEEEauthorblockN{2\textsuperscript{nd} Given Name Surname}
	\IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
		\textit{name of organization (of Aff.)}\\
		City, Country \\
		email address or ORCID}
	\and
	\IEEEauthorblockN{3\textsuperscript{rd} Given Name Surname}
	\IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
		\textit{name of organization (of Aff.)}\\
		City, Country \\
		email address or ORCID}
	\and
	\IEEEauthorblockN{4\textsuperscript{th} Given Name Surname}
	\IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
		\textit{name of organization (of Aff.)}\\
		City, Country \\
		email address or ORCID}
	\and
	\IEEEauthorblockN{5\textsuperscript{th} Given Name Surname}
	\IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
		\textit{name of organization (of Aff.)}\\
		City, Country \\
		email address or ORCID}
	\and
	\IEEEauthorblockN{6\textsuperscript{th} Given Name Surname}
	\IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
		\textit{name of organization (of Aff.)}\\
		City, Country \\
		email address or ORCID}
}

\maketitle

\begin{abstract}
	Pierce's Disease is a becterial disease caused by Xylella fastidiosa. It affects the grapevine leaves. Accurate assessment of grapevine leaf health is important for precision agriculture. But traditional manual inspection is higly subjective and often inconsistent. Recent advances in \ac{dl} have enabled automated analysis of grapevine leaves. In this paper, we propose \textit{PierceStageNet}, a transfer learning model optimized for the multi-stage grapevine Pierce's Disease classification on a relatively small dataset. The model uses an \textit{EfficientNet-B0} backbone, enhanced with a \ac{se} attention module, and a compact classification head. It effectively extracts grapevine leaves feature for a better classification of disease stage. Our model achieves a training 0.9751 accuracy, 0.9754 precision, 0.9750 recall, and 0.9749 F1-score, significantly outperforming baseline \textit{EfficientNet-B0} on PierceDisease Dataset. These results highlight the model's potential to facilitate timely disease detection. It bridges the gap between baseline research models and practical precision agriculture applications.
\end{abstract}

\begin{IEEEkeywords}
	Deep Learning, Computer Vision, Grapevine, Pierce Disease, EfficientNet, SE Attention
\end{IEEEkeywords}

\begin{figure*}[htbp]
	\centerline{\includegraphics[width=\textwidth]{images/methodology.png}}
	\caption{Methodology of the proposed PierceStageNet: grapevine disease stage classification framework.}
	\label{fig:methodology}
\end{figure*}

\section{Introduction}
Pierce's Disease in grapevines is a fatal disease caused by the bacterium Xylella fastidiosa. This becterium hat lives in the xylem of grapevines. It blocks water flow and causes symptoms like leaf scorch, wilting, and eventual vine death. It spreads by insects like the \textit{Glassy Winged Sharpshooter}. Grapevine health and fruit quality are critical measures of vineyard productivity and quality.
Recent climate studies indicate an increasing global risk of grapevine Pierce’s disease. Analyses of over 100,000 vineyards show that the area at risk in Europe has increased from 22\% to over 41\%. While in South Africa it has increased from roughly 6\% to more than 47\% \cite{GimenezRomero2025}. Global warming may also cause the spread of Pierce’s disease  into major European wine growing regions \cite{GimenezRomero2024}.

Making early disease detection and accurate assessment of leaf conditions is essential for precision viticulture Traditionally, plant leaf disease monitoring has been done through manual inspection. Manual inspection relies completely on human observation. Different inspectors may interpret the same symptoms in different ways that leads to inconsistent conclusions about leaf health. Traditional evaluation methods do not follow any uniform standard measure. These methods are also very time consuming. Only a small part of the vineyard can be examined manually due to resource constraints.

Recent advances in \ac{cv} and \ac{dl} have transformed this domain by enabling automated analysis of grapevine leaves through high resolution images. In particular, \ac{dl} models like \ac{cnn}s, have showed rreliable performance in detecting and classifying morphological and spectral patterns associated with leaf health and disease/ripeness stages. Existing studies highlight the efficiency of \ac{cv} approaches. \ac{cnn} classification models have achieved high accuracies.  Despite these advancements, many challenges remain. Most existing models are either computationally intensive or rely on large datasets. This limits their applicability in small vineyards. Many studies focus only on binary disease detection or single-stage ripening classification. Neglecting the nuanced multi-stage progression of grapevine leaves disease. Real-time deployability and lightweight architectures suitable for edge devices are rarely addressed, leaving a gap between research and practical vineyard implementation.

To address these gaps, this paper proposes \textit{PierceStageNet}, a transfer learning model optimized for multi-stage grapevine leaf Pierce's Disease classification on a relatively small dataset. The model uses a frozen \textit{EfficientNet-B0} \cite{pmlr-v97-tan19a} backbone with selective feature extraction. It is enhanced by a lightweight \ac{se} attention \cite{Hu_2018_CVPR} module, and a compact classification head. By fine-tuning only a subset of the \textit{EfficientNet-B0} blocks, \textit{PierceStageNet} achieves accuracy of approximately 97\%. While  also maintained a low computational cost that makes it suitable for real-time vineyard applications on edge devices. We also add model explainability with \ac{gradcam}. The key contributions of this paper include:
\begin{itemize}
	\item A multi-stage grapevine leaf Pierce's Disease classification model.
	\item Integration of a \ac{se} attention mechanism to enhance feature representation and improve stage wise disease classification accuracy.
	\item Robust performance on training, validation, and test splits, achieving a high accuracy on all splits that indicates generalizability to unseen data.
	\item Explainability of model for enhanced trustworthiness.
\end{itemize}

The rest of the paper is structured as follows. Section \ref{sec:rw} reviews most recent and relevant literature on grapevine leaf disease detection, including \ac{dl} architectures, attention mechanisms, and classical machine learning approaches. Section \ref{sec:methodology} details the materials and methods, covering dataset description, preprocessing pipelines, model architecture, and training strategy. Section \ref{sec:results} presents experimental results, performance evaluation. Section \ref{sec:comparison} comparative analysis with baseline pretrained models. Seciton \ref{sec:limitations} provides limitations, and Section~\ref{sec:future} gives directions for future work. Finally, Section \ref{sec:con} concludes the paper with key findings.

\section{Related Work}
\label{sec:rw}
Grapevine leaf disease detection has seen significant progress over the past decade. Advancements in \ac{dl}, \ac{ml}, and spectroscopic analysis. Among all, \ac{cnn}s have emerged as the most effective approach withh their capability of extracting spatial, textural, and morphological features from complex leaf images. For instance, \cite{Malagol2025} applied a ResNet based architecture to quantify hair structures on grape leaves with an accuracy of 95.41\%. Other than classification, segmentation of diseased regions has also been explored to focus on biologically relevant features only. Study \cite{Zhang2024GMamba} used a UNet state space model on PlantVillage dataset, achieving an \ac{iou} of 3.28. Object detection methods have also facilitated the on-field applications by enabling automated identification of diseased areas in leafs. \cite{Khan2025YOLOv7} reported a \ac{map} of 64.6\%, while a more recent YOLOv11 approach \cite{Zhou2025YOLOGrape} improved detection performance with a \ac{map} of 95.2\%.

Classification tasks have also reached strong levels of performance. ResNet models have showed stable and consistent behavior, \cite{Sagar2025} achieved 95\% accuracy on grape leaf datasets. Methods that fuse several backbone networks have further improved accuracy by using feature information from different architectures. For example, \cite{Vo2026AFCNet} combined \textit{ResNet50}, EfficientNetB0, and MobileNetV2, resulting in 99.83\% accuracy. Which shows that the use of different feature extractors together can enhance classification accuracy. Models that integrate transformer architectures with convolutional layers have also shown reliable results \cite{Zhang2025Transformer} gained 98.48\% accuracy. Similarly, cascaded \ac{cnn} models, as proposed by \cite{AlNasseri2025}; approached near perfect classification with 100\% accuracy. Efficient and compact models designed for faster inference have gained significant attention for edge devices. MobileNetV2 combined with\ac{vit} modules \cite{Tanvir2025} had strong outcomes with 99.2\% accuracy. While comparative evaluations \cite{Mathew2025} reported 99.5\% accuracy. \ac{dcnn} based on VGG16 \cite{Prasad2024} reached 99.18\% accuracy. Showing that classical architectures can still offer competitive performance. Modified \ac{cnn} models have further strengthened classification tasks, study \cite{Talaat2025} reported 99.7\% accuracy and \cite{Karim2024} reached 99.66\%. Even simpler refined \ac{cnn} approaches \cite{Naik2025} have stable and dependable performance across multiple grape leaf disease datasets.

Beyond \ac{dl} methods, classical \ac{ml} methods also provide complementary insights that are reliable for nutrient assessment and early pathogen detection. \ac{svm}s combined with feature optimization  were applied by \cite{Shi2025} to classify fungal spores. They report 97.5\% accuracy, highlighting the usefulness of traditional \ac{ml} techniques when paired with domain specific feature engineering. Chemometric regression models have been successfully used for predicting macro and micro-nutrient concentrations, \cite{Manzano2025} reported an R² of 0.90 for sodium content. Early detection of viral infections has been made possible through hyperspectral and multispectral sensing techniques. As presented in paper \cite{Lee2024} where strong infections were identified at CT values below 35.

These studies show that combining \ac{cnn}, hybrid transformers, multi-backbone fusion, and feature based \ac{ml} provides accurate solutions for grape leaf disease detection, nutrient assessment, and pathogen monitoring. Edge deployable models with explainable AI techniques like \ac{gradcam} enable practical precision viticulture. They support the rapid disease assessment and optimized vineyard management. Spectroscopic and chemometric methods complement imaging for non destructive plant health monitoring.

\begin{figure*}[htbp]
	\centerline{\includegraphics[width=\textwidth]{images/model.png}}
	\caption{Proposed Architecture of \textit{PierceStageNet} with \textit{EfficientNet-B0} backbone and external \ac{se} attention block.}
	\label{fig:model}
\end{figure*}

\section{Material and Methods}
\label{sec:methodology}
This study implements a modified EfficientNetB0 \ac{dl} model for detecting and classifying Pierce's Disease stages in grapevine leaves. The methodology follows five steps: Data acquisition, Preprocessing, Classification and evaluation, Results analysis, and Comparative assessment as shown in Figure~\ref{fig:methodology}. The complete pipeline was implemented in Python. We used PyTorch and executed model on Google Colab with GPU acceleration.

\subsection{Data Description}
The dataset consists of labeled images of grapevine leaves organized into four classes. Each class representing progressive stage of Pierce's Disease. The classification includes Fresh leaves, Stage 1 having early infection, Stage 2 with moderate progression, and Stage 3 is advanced infection in leaves. Figure~\ref{fig:datasamples} shows data samples from training subset. Images were sourced from the ``PierceDisease: Xylella fastidiosa affected Grape Leaves comprehensive image dataset'' \cite{Islam2025PierceDisease} dataset, which provides four progression stages of Pierce's Disease. We split dataset into Train, Test and Validaton subsets. The dataset contains 4,004 images in total. These images were distributed as: 3,203 training images, 400 validation images, and 401 test images. Table~\ref{tab:data_distribution} shows the data distribution. As shown in Listing~\ref{lst:classmapping}, a consistent integer mapping was established for standard label encoding across all subsets.

\begin{figure}[htbp]
	\centering
	\begin{subfigure}[b]{0.22\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{images/fresh.png}
		\caption{Fresh Leaf}
		\label{fig:fresh}
	\end{subfigure}
	\begin{subfigure}[b]{0.22\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{images/stage1.png}
		\caption{Stage 1}
		\label{fig:stage1}
	\end{subfigure}
	\begin{subfigure}[b]{0.22\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{images/stage2.png}
		\caption{Stage 2}
		\label{fig:stage2}
	\end{subfigure}
	\begin{subfigure}[b]{0.22\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{images/stage3.png}
		\caption{Stage 3}
		\label{fig:stage3}
	\end{subfigure}
	\caption{Samples from Grapevine Pierce's Disease dataset.}
	\label{fig:datasamples}
\end{figure}


\begin{figure*}[htbp]
    \centering
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/fresh_original.png}
        \caption{Fresh Leaf}
        \label{fig:fresh_orig}
    \end{subfigure}
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/s1_original.png}
        \caption{Stage 1}
        \label{fig:stage1_orig}
    \end{subfigure}
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/s2_original.png}
        \caption{Stage 2}
        \label{fig:stage2_orig}
    \end{subfigure}
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/s3_original.png}
        \caption{Stage 3}
        \label{fig:stage3_orig}
    \end{subfigure}
    \vspace{0.5em}

    % Augmented Images
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/fresh_aug.png}
        \caption{Fresh Augmented}
        \label{fig:fresh_aug}
    \end{subfigure}
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/s1_aug.png}
        \caption{Stage 1 Augmented}
        \label{fig:stage1_aug}
    \end{subfigure}
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/s2_aug.png}
        \caption{Stage 2 Augmented}
        \label{fig:stage2_aug}
    \end{subfigure}
    \begin{subfigure}[b]{0.18\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/s3_aug.png}
        \caption{Stage 3 Augmented}
        \label{fig:stage3_aug}
    \end{subfigure}

    \caption{Examples of original (top row) and augmented (bottom row) training images.}
    \label{fig:datasamples_combined}
\end{figure*}

\begin{table}[h!]
	\caption{Distribution of images across dataset splits for grapevine Pierce's Disease stages.}
	\centering
	\begin{tabular}{m{0.2\columnwidth} m{0.2\columnwidth} m{0.2\columnwidth} m{0.2\columnwidth}}
		\toprule
		\textbf{Stage} & \textbf{Train} & \textbf{Validation} & \textbf{Test} \\
		\midrule
		Fresh          & 800            & 100                 & 100           \\
		Stage 1        & 800            & 100                 & 100           \\
		Stage 2        & 800            & 100                 & 100           \\
		Stage 3        & 803            & 100                 & 101           \\
		\midrule
		\textbf{Total} & \textbf{3203}  & \textbf{400}        & \textbf{401}  \\
		\bottomrule
	\end{tabular}
	\label{tab:data_distribution}
\end{table}

\begin{lstlisting}[style=ieeelisting, frame=none,
caption={Dataset Classes mapping},
label={lst:classmapping}]
Classes:
['Fresh', 'Stage1', 'Stage-2', 'Stage-3']

Class mapping:
{'Fresh': 0, 'Stage1': 1, 'Stage-2': 2, 'Stage-3': 3}
\end{lstlisting}

\subsection{Pre-processing}
Two different preprocessing pipelines were implemented. One for training data for augmentation. Other for validation and testing with minimal transformations. Training subset transformations include resizing images to 224×224 px, random horizontal and vertical flipping, ±20-degree rotation, color jittering, random affine transformations, and ImageNet normalization. Validation and test subset transformations consisted only of resizing, tensor conversion, and normalization. All the transformations are applied during training, on the go. This strategy enhances model generalization and also maintains evaluation consistency. The augmented images  given by the dataset providers were not used as they had extensive and diverse transformations; 40,000 images in total generated from 4,004 raw images. This could lead to overfiting on synthetic data rather than learning features from the real data. Instead we did minimal augmentation on the fly while training the model to keep model near to real-world deployment. Figure~\ref{fig:datasamples_combined} shows some of preprocessing transformations.

\begin{figure*}[htbp]
	\centerline{\includegraphics[width=\textwidth]{images/backbone.png}}
	\caption{{EfficientNet-B0} backbone for \textit{PierceStageNet}.}
	\label{fig:efficientnetb0}
\end{figure*}

\subsection{Model Training and Evaluation}
The primary model, \textit{PierceStageNet}, is based on \textit{EfficientNet-B0}. It uses transfer learning. As shown in Algorithm~\ref{alg:PierceStageNet}, the architecture integrates a lightweight \ac{se} module to enhance feature discrimination for disease classification. At the end is a custom classification head for final predictions. Figure~\ref{fig:model} illustrates the overall model structure. For benchmarking, standard \textit{EfficientNet-B0} architecture was implemented under identical conditions as our proposed model.
\begin{algorithm}
	\caption{PierceStageNet Training, Validation, and Testing.}
	\label{alg:PierceStageNet}
	\begin{algorithmic}[1]
		\STATE \textbf{Input:} Image batch $I$
		\STATE \textbf{Output:} Predicted disease class labels

		\STATE Load pretrained \textit{EfficientNet-B0} backbone
		\STATE Insert SE attention module
		\STATE Construct classifier head

		\FOR{each training batch $I$}
		\STATE $x \gets \texttt{Backbone}(I)$
		\STATE $x \gets \texttt{SE}(x) \odot x$
		\STATE $y \gets \texttt{Classifier}(x)$
		\STATE Compute cross-entropy loss
		\STATE Update trainable parameters
		\ENDFOR

		\STATE Evaluate model on validation dataset
		\STATE Evaluate model on test dataset
		\STATE Return trained PierceStageNet model and test predictions
	\end{algorithmic}
\end{algorithm}

\subsubsection{Backbone}
The backbone of the model is based on \textit{EfficientNet-B0}. It provides feature extraction from input images. The model uses pretrained ImageNet weights to retain generic visual features while also adapting to the target dataset.  Backbone shown in Figure~\ref{fig:efficientnetb0} and Listing~\ref{lst:backbone}. This strategy preserves low-level features learned from large ImageNet dataset and also focuses on adapting to the specific characteristics of our grapevine dataset. Improves convergence and reduces the risk of model overfitting.

\begin{lstlisting}[style=ieeelisting, frame=none,
caption={Backbone Initialization of PierceStageNet},
label={lst:backbone}, basicstyle=\ttfamily\footnotesize]
Input: Images batch X

Initialize EfficientNet-B0
Divide the backbone into ordered feature blocks

Output: Feature map F with C channels
\end{lstlisting}

\subsubsection{\ac{se} Block}
Next part is Sequeeze-and-Excitation block \cite{Hu_2018_CVPR}.The \ac{se} block is designed to improve the representational power of our model by explicitly modeling channel-wise interdependencies. It is applied to final feature map and emphasizes the most important channels for and improved classification. \ac{se} attention allows model to suppress the pretrained ImageNet channels and focus on our dataset. \ac{se} block structure is given in Listing~\ref{lst:semodule} and Figure~\ref{fig:semodule}. It consists of two main operations:

\begin{itemize}
	\item \textbf{Squeeze:} Global average pooling is applied to each feature map to generate the channel-wise descriptor:
	      \begin{equation}
		      z_c = \frac{1}{H \times W} \sum_{i=1}^{H} \sum_{j=1}^{W} X_c(i,j)
	      \end{equation}
	      where \(X_c \in \mathbb{R}^{H \times W}\) is the \(c\)-th channel of the feature map, and \(z_c\) is its global descriptor.

	\item \textbf{Excitation:} The channel descriptors are passed through a small, two layer fully connected network with a non-linear activation and a sigmoid to generate channel-wise weights:
	      \begin{equation}
		      s = \sigma(W_2 \delta(W_1 z))
	      \end{equation}
	      where \(W_1\) and \(W_2\) are learnable weight matrices, \(\delta\) is the ReLU activation, and \(\sigma\) is the sigmoid function.

	\item \textbf{Recalibration:} Finally, the original feature maps are rescaled using these learned weights:
	      \begin{equation}
		      \tilde{X}_c = s_c \cdot X_c
	      \end{equation}
\end{itemize}
\begin{figure}[h!]
	\centerline{\includegraphics[width=\columnwidth]{images/se_attention.png}}
	\caption{\textit{PierceStageNet} \ac{se} block structure.}
	\label{fig:semodule}
\end{figure}

Although \textit{EfficientNet-B0} contains \ac{se} blocks in MBConv block which performs local channel recalibration, we added an additional trainable \ac{se} module at the feature head for better adaptation to our dataset. \ac{se} module configuration is given in Tabel~\ref{tab:se_configuration}. This module performs task-specific channel recalibration on the final convolutional feature map. The internal \ac{se} works within blocks for recalibrating intermediate feature maps. External \ac{se} operates on the backbone output feature map and provides a global perspective over all channels. \ac{se} provides feature refinement with minimal parameter overhead and increases model accuracy. Adding an external \ac{se} module improved model accuracy by ~2-3\% over baseline \textit{EfficientNet-B0}, global channel recalibration complements internal \ac{se}. As given in Section~\ref{sec:results}.

\begin{lstlisting}[style=ieeelisting, frame=none,
caption={\ac{se} Module},
label={lst:semodule}, basicstyle=\ttfamily\footnotesize]
Input: Feature map F with C channels

Apply global average pooling
Reduce channels to C/r, apply ReLU, restore to C
Apply sigmoid and scale F by attention weights

Output: Recalibrated feature map
\end{lstlisting}

\begin{table}[h!]
    \caption{SE attention module configuration for \textit{PierceStageNet}}.
    \centering
    \begin{tabular}{lc}
        \toprule
        \textbf{Parameter} & \textbf{Value} \\
        \midrule
        Channels           & 1280 \\
        Reduction ratio    & 16 \\
        Pooling            & 1 × 1 \\
        Reduce channels    & 80 \\
        Activation         & ReLU \\
        Restore channels   & 1280 \\
        Gating             & Sigmoid \\
        \bottomrule
    \end{tabular}
    \label{tab:se_configuration}
\end{table}

\subsubsection{Classification Head}
The last part is classification head. Classification head maps the attended feature maps to final predictions. It includes a fully connected layer, ReLU activation, dropout for regularization. At last, a final linear layer for the output logits corresponding to the target classes. Given in Listing~\ref{lst:classhead}. Classification head configuration is given in Table~\ref{tab:classification_head_configuration}.

\begin{figure}[h!]
	\centerline{\includegraphics[width=\columnwidth]{images/classification_head.png}}
	\caption{\textit{PierceStageNet} Classification head structure.}
	\label{fig:semodule}
\end{figure}

\begin{lstlisting}[style=ieeelisting, frame=none,
caption={Classification Head},
label={lst:classhead}, basicstyle=\ttfamily\footnotesize]
Input: Recalibrated feature map

Apply global average pooling and flatten
Fully connected layer with D units, ReLU activation, and dropout
Fully connected layer to output class logits (K classes)

Output: Class logit scores
\end{lstlisting}

\begin{table}[h!]
    \caption{Classification head configuration for \textit{PierceStageNet}.}
    \centering
    \begin{tabular}{lc}
        \toprule
        \textbf{Layer / Parameter} & \textbf{Value} \\
        \midrule
        Pooling Type        & Global Average Pooling \\
        Pooling Output Size & 1 $\times$ 1 \\
        Flatten             & Yes \\
        Input Features      & 1280 \\
        Hidden Layer Units  & 512 \\
        Activation          & ReLU \\
        Dropout             & 0.3 \\
        Output Layer        & Fully Connected \\
        Output Features     & 4 \\
        \bottomrule
    \end{tabular}
    \label{tab:classification_head_configuration}
\end{table}

\subsubsection{Forward Pass}
During the forward pass, the input images first pass through the backbone to extract feature maps. Then \ac{se} module computes channel-wise attention. Finally, the classification head processes those features and gives logits for the class predictions. Shown in Listing~\ref{lst:fpass}.

\begin{lstlisting}[style=ieeelisting, frame=none,
caption={Forward Pass of PierceStageNet},
label={lst:fpass}, basicstyle=\ttfamily\footnotesize]
Input: Image batch X

Extract features using EfficientNet-B0 backbone
Apply SE module and reweight features with attention
Pass features through classification head

Output: Class logit scores
\end{lstlisting}

\subsection{Evaluation Metrics}
Model performance was assessed using standard classification metrics accuracy, precision, recall and f1-score. The macro-averaging approach was used to give equal weight to all classes.

\begin{itemize}
	\item \textbf{Accuracy}: Proportion of correct predictions out of all predictions of model:
	      \begin{equation}
		      \text{Accuracy} = \frac{1}{N} \sum_{i=1}^{N} \frac{TP_i + TN_i}{TP_i + TN_i + FP_i + FN_i}
	      \end{equation}

	\item \textbf{Precision}: Measure how many positive predictions of model are actually correct:
	      \begin{equation}
		      \text{Precision} = \frac{1}{N} \sum_{i=1}^{N} \frac{TP_i}{TP_i + FP_i}
	      \end{equation}

	\item \textbf{Recall}: Ratio of correctly predicted positive samples to all actual positive samples:
	      \begin{equation}
		      \text{Recall} = \frac{1}{N} \sum_{i=1}^{N} \frac{TP_i}{TP_i + FN_i}
	      \end{equation}

	\item \textbf{F1-Score}: Harmonic mean of precision and recall for each class:
	      \begin{equation}
		      \text{F1-Score} = \frac{1}{N} \sum_{i=1}^{N} \frac{2 \cdot \text{Precision}_i \cdot \text{Recall}_i}{\text{Precision}_i + \text{Recall}_i}
	      \end{equation}
\end{itemize}

In addition to this, confusion matrix was also visualized to analyze classification patterns. These statistics are given in Section~\ref{sec:results}.

\section{Experiment Results}
\label{sec:results}
\textit{PierceStageNet} provides strong and consistent results on training, validation, and test datasets. It outperforms baseline architectures \textit{EfficientNet-B0}, \textit{MobileNetV3Small} and \textit{ResNet-50} on our dataset.

\subsection{Experiment Setting}
The model is initialized and moved to the available GPU device. We used google colab. The model was trained for up to 20 epochs. We used an ImageNet pretrained \textit{EfficientNet-B0} backbone combined with an \ac{se} module, custom classification head, optimized using AdamW. The pretrained backbone was fine-tuned with a learning rate of $1\times10^{-4}$. The classifier trained at $1\times10^{-3}$.  Cross-entropy loss applied with label smoothing of 0.1. We used OneCycle learning rate scheduler with a 40\% warm up period and cosine annealing. Mixed precision training accelerated computations and reduced the memory usage. Early stopping implemented with a patience of 5 epochs to prevent overfitting. The best performing epoch model was selected based on higher validation accuracy.

Training and validation metrics recorded for each epoch; including accuracy, precision, recall, and F1-score.

\subsection{\textit{PierceStageNet} With Pretrained Backbone}
The proposed PierceStageNet trained for 20 epochs with ImageNet1k pretrained weights. Model has 4,871,632 Total params, 4,871,632 Trainable params and 0 Non-trainable params. The model continuously improved in both training and validation performance reaching 0.9835 train and 0.9425 validation accuracies at its best. Evaluation on the test set gave comparable results. Model had test accuracy 0.9751, precision of 0.9754, 0.9750 recall and F1-Score 0.9749.The model had strong generalization performance. The final evaluation metrics are summarized in Table~\ref{tab:PierceStageNetPretrained_metrics}, Table~\ref{tab:PierceStageNetPretrained_class_metrics} and Figure~\ref{fig:PierceStageNetPretrained_performance}.

\begin{table}[H]
	\caption{Test results for \textit{PierceStageNet} (ImageNet1k pretrained).}
	\centering
	\begin{tabular}{m{0.2\columnwidth} m{0.2\columnwidth} m{0.2\columnwidth} m{0.2\columnwidth} m{0.1\columnwidth}}
		\toprule
		\textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
		\midrule
		0.9751     & 0.9754    & 0.9750    & 0.9749      \\
		\bottomrule
	\end{tabular}
	\label{tab:PierceStageNetPretrained_metrics}
\end{table}

\begin{table}[H]
	\caption{Classwise Test results for \textit{PierceStageNet} (ImageNet1k pretrained).}
	\centering
	\begin{tabular}{m{0.3\columnwidth} m{0.1\columnwidth} m{0.1\columnwidth} m{0.1\columnwidth} m{0.1\columnwidth}}
		\toprule
		\textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Support} \\
		\midrule
		Fresh     & 0.9901    & 1.0000    & 0.9950     & 100          \\
		Stage 1   & 0.9800    & 0.9800    & 0.9800     & 100           \\
		Stage 2   & 0.9789    & 0.9300    & 0.9538     & 100           \\
		Stage 3   & 0.9524    & 0.9901    & 0.9709     & 101           \\
		\bottomrule
	\end{tabular}
	\label{tab:PierceStageNetPretrained_class_metrics}
\end{table}

Class-wise evaluation indicates strong results across all disease stages. Fresh class shows no missclassifications. Small number of misclassifications occurred between similar disease stages: Stage 2 and Stage 3. So errors are systematic and are linked to visual similarities between these two stages not random predictions. The confusion matrix for the test set given in Figure~\ref{fig:cmatrix_pretrained_PierceStageNet} provides insights into misclassifications between adjacent disease stages. Supporting the claim that errors are primarily due to visual resemblance rather than random noise. This reveals the model's logical performance in disease stage classification.

\begin{figure}[h!]
	\centerline{\includegraphics[width=0.7\columnwidth]{images/pretrained_PSNet.png}}
	\caption{Confusion matrix for \textit{PierceStageNet} (ImageNet1k pretrained) on test dataset with true and predicted class labels.}
	\label{fig:cmatrix_pretrained_PierceStageNet}
\end{figure}

Overall, the model has reliable performance for grapevine disease stage classification. Given a high accuracy, precision, recall, and F1-scores across all subsets. Figure~\ref{fig:testsamples_pretrained_PierceStageNet} provides more details about model's classifications including correct and wrong predictions with leaf images.

\subsection{\textit{PierceStageNet} Without Pretrained Backbone}
Model trained for 40 epochs as it has no pretrained weights. This model has same number of parameters as previous pretrained one. Precision, recall, and F1-score followed a similar trend, with the model achieving an accuracy of 0.8504, precision 0.8528, recall 0.8502 and F1-Score 0.8506. Results were lower than previous with pretrained ImageNet1k weights. The final evaluation metrics are summarized in Table~\ref{tab:PierceStageNet_notPretrained_metrics}, Table~\ref{tab:PierceStageNet_notPretrained_class_metrics} and Figure~\ref{fig:performance_notPretrained}.

\begin{table}[H]
	\caption{Test results for \textit{PierceStageNet} (not pretrained).}
	\centering
	\begin{tabular}{m{0.2\columnwidth} m{0.2\columnwidth} m{0.2\columnwidth} m{0.2\columnwidth} m{0.1\columnwidth}}
		\toprule
		\textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
		\midrule
		0.8504     & 0.8528   & 0.8502   & 0.8506    \\
		\bottomrule
	\end{tabular}
	\label{tab:PierceStageNet_notPretrained_metrics}
\end{table}

\begin{table}[H]
	\caption{Classwise Test results for \textit{PierceStageNet} (not pretrained).}
	\centering
	\begin{tabular}{m{0.3\columnwidth} m{0.1\columnwidth} m{0.1\columnwidth} m{0.1\columnwidth} m{0.1\columnwidth}}
		\toprule
		\textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Support} \\
		\midrule
		Fresh     & 0.9468    & 0.8900    & 0.9175    & 100         \\
		Stage 1   & 0.7636    & 0.8400    & 0.8000    & 100         \\
		Stage 2   & 0.7979    & 0.7500    & 0.7732    & 100          \\
		Stage 3   & 0.7979    & 0.7500    & 0.7732    & 101          \\
		\bottomrule
	\end{tabular}
	\label{tab:PierceStageNet_notPretrained_class_metrics}
\end{table}

Class-wise evaluation indicates almost same trend as the model with pretrained weights. Misclassifications mostly occurred between adjacent stages.The confusion matrix for the test set given in Figure~\ref{fig:cmatrix_notPretrained} provides insights into results.

\begin{figure}[H]
	\centerline{\includegraphics[width=0.7\columnwidth]{images/nopretrained_PSNet.png}}
	\caption{Confusion matrix for \textit{PierceStageNet} (no pretrained weights) on test dataset with true and predicted class labels.}
	\label{fig:cmatrix_notPretrained}
\end{figure}

Figure~\ref{fig:testsamples_notPretrained_PierceStageNet} provides more information about pretrained \textit{PierceStageNet}  model's classifications including correct and wrong predictions with leaf images.
\begin{figure*}
	\centering
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/accuracy_plot_se0.png}
		\caption{Accuracy}
		\label{fig:accuracy3}
	\end{subfigure}
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/precision_plot_se0.png}
		\caption{Precision}
		\label{fig:precision3}
	\end{subfigure}
	\vspace{0.5em}
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/recall_plot_se0.png}
		\caption{Recall}
		\label{fig:recall3}
	\end{subfigure}
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/f1_plot_se0.png}
		\caption{F1-score}
		\label{fig:f13}
	\end{subfigure}

	\caption{Training and validation curves for \textit{PierceStageNet} (ImageNet1k pretrained).}
	\label{fig:PierceStageNetPretrained_performance}
\end{figure*}
\begin{figure*}
	\centering
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/accuracy_plot_se_no_pretrained_wieghts.png}
		\caption{Accuracy}
		\label{fig:accuracy4}
	\end{subfigure}
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/precision_plot_se_no_pretrained_wieghts.png}
		\caption{Precision}
		\label{fig:precision4}
	\end{subfigure}
	\vspace{0.5em}s
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/recall_plot_se_no_pretrained_wieghts.png}
		\caption{Recall}
		\label{fig:recall4}
	\end{subfigure}
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{images/f1_plot_se_no_pretrained_wieghts.png}
		\caption{F1-score}
		\label{fig:f14}
	\end{subfigure}

	\caption{Training and validation curves for \textit{PierceStageNet} (not pretrained).}
	\label{fig:performance_notPretrained}
\end{figure*}

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/pretrained_PSNet_samples.png}
    \caption{Test prediction samples for \textit{PierceStageNet} (ImageNet1k pretrained backbone).}
    \label{fig:testsamples_pretrained_PierceStageNet}
\end{figure*}

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/nopretrained_PSNet_samples.png}
    \caption{Test prediction samples for \textit{PierceStageNet} (without pretrained backbone).}
    \label{fig:testsamples_notPretrained_PierceStageNet}
\end{figure*}

% Figure 1: Pretrained
\begin{figure*}[!htbp]
    \centering
    % Pretrained - correct
    \includegraphics[width=\textwidth]{images/gradcam_correct_pretrainedPSNet.png}
    \caption*{\textit{PierceStageNet} (ImageNet1K pretrained) representative correct test samples with \ac{gradcam} overlay}
    \vspace{1em}

    % Pretrained - wrong
    \includegraphics[width=\textwidth]{images/gradcam_wrong_pretrained_PSNet.png}
    \caption*{\textit{PierceStageNet} (ImageNet1K pretrained) representative wrong test samples with \ac{gradcam} overlay}

    % Main caption
    \caption{\ac{gradcam} explainability for \textit{PierceStageNet} (pretrained) on representative test images. Original images are shown with the \ac{gradcam} heatmap overlay. Regions most influential for the model's prediction are highlighted in red.}
    \label{fig:gradcam_explainability_pretrained}
\end{figure*}

% Figure 2: Not pretrained
\begin{figure*}[!htbp]
    \centering
    % Not pretrained - correct
    \includegraphics[width=\textwidth]{images/gradcam_correct_nopretrainedPSNet.png}
    \caption*{\textit{PierceStageNet} (not pretrained) representative correct test samples with \ac{gradcam} overlay}
    \vspace{1em}

    % Not pretrained - wrong
    \includegraphics[width=\textwidth]{images/gradcam_wrong_nopretrainedPSNet.png}
    \caption*{\textit{PierceStageNet} (not pretrained) representative wrong test samples with \ac{gradcam} overlay}

    % Main caption
    \caption{\ac{gradcam} explainability for \textit{PierceStageNet} (not pretrained) on representative test images. Original images are shown with the \ac{gradcam} heatmap overlay. Regions most influential for the model's prediction are highlighted in red.}
    \label{fig:gradcam_explainability_nopretrained}
\end{figure*}

% efficientnetb0
\begin{figure*}[h!]
	\centering
	\includegraphics[width=0.9\textwidth]{images/eb0_pretrained.png}
	\caption{Learning curves for \textit{EfficientNet-B0} (ImageNet1K Pretrained).}
	\label{fig:eb_pretrained}
\end{figure*}
\begin{figure*}[h!]
	\centering
	\includegraphics[width=0.9\textwidth]{images/eb0_not_pretrained.png}
	\caption{Learning curves for \textit{EfficientNet-B0} (not Pretrained).}
	\label{fig:eb_not_pretrained}
\end{figure*}

\subsection{Model Explainability with \ac{gradcam}}
We used \ac{gradcam} \cite{Selvaraju_2017_ICCV} to interpret the predictions of \textit{PierceStageNet}. \ac{gradcam} generates visual explanations by highlighting regions in the input images that are most influential in prediction. For our analysis, we selected one representative test image from each class predictions and generated \ac{gradcam} overlays on the top of original images. The overlays show that the model consistently focuses on the leaf regions relevant to the disease stages. It confirms that the network relies on meaningful visual features. Figure~\ref{fig:gradcam_explainability_pretrained} and \ref{fig:gradcam_explainability_nopretrained} provides the original and Grad-CAM heatmaps side by side for a clear visual understanding of the attention of the model. Thereby improves explainability and trustworthiness on model predictions. Correct and missclassified test samples are give for both \textit{PierceStageNet} models.

\section{Model Comparison and Analysis}
\label{sec:comparison}
To assess the effectiveness of \textit{PierceStageNet}, we made a comparison with baseline model \textit{EfficientNet-B0}. The baseline models was trained in same conditions as \textit{PierceStageNet} models. We used ImageNet pretrained weights, the AdamW optimizer, and a similar training schedule for up to 20 epochs on the same training and validation subsets. The final test results indicate that \textit{PierceStageNet} outperforms \textit{EfficientNet-B0} model both with and without pretrained ImageNet1K weights.
\subsection{Comparison with baseline models}
\textit{EfficientNet-B0} ImageNet1K pretrained, had a test accuracy of 0.9501 with precision 0.9507, recall 0.9501, and F1-score 0.9500.In contrast, \textit{PierceStageNet} had 0.9751 accuracy, 0.9754 precision, 0.9750 recall, and 0.9749 F1-score.
\textit{EfficientNet-B0} with no pretrained weights, scored test accuracy of 0.8329, precision 0.8416, recall 0.8329, and F1-score 0.8337. While, \textit{PierceStageNet} without pretrained weights had 0.8504 accuracy, 0.8528 precision, .8502 recall, and 0.8506 F1-score. Detailed comparison given in Table~\ref{tab:eb_metrics} and \ref{tab:comparison_all_models}

\begin{table}[H]
	\caption{Test results for \textit{EfficientNetB0}.}
	\centering
	\begin{tabular}{m{0.2\columnwidth} m{0.14\columnwidth} m{0.14\columnwidth} m{0.14\columnwidth} m{0.14\columnwidth}}
		\toprule
		\textbf{Model} &\textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
		\midrule
		EfficientNetB0 (Pretained)         & 0.9501            & 0.9507           & 0.9501          & 0.9500    \\
		EfficientNetB0 (Without Pretraining)& 0.8329           & 0.8416             & 0.8329          & 0.8506    \\
		\bottomrule
	\end{tabular}
	\label{tab:eb_metrics}
\end{table}

\begin{table*}[t]
	\caption{Comparison of test results for \textit{EfficientNetB0} and \textit{PierceStageNet} with and without ImageNet1K pretraining.}
	\centering
	\begin{tabular}{m{0.35\textwidth} m{0.12\textwidth} m{0.12\textwidth} m{0.12\textwidth} m{0.12\textwidth}}
		\toprule
		\textbf{Model} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
		\midrule
		EfficientNetB0 (ImageNet1K Pretained)         & 0.9501            & 0.9507           & 0.9501          & 0.9500    \\
		\textbf{PierceStageNet (ImageNet1K pretrained)} & \textbf{0.9751} & \textbf{0.9754} & \textbf{0.9750} & \textbf{0.9749} \\
		EfficientNetB0 (Without Pretraining)& 0.8329           & 0.8416             & 0.8329          & 0.8506    \\
		\textbf{PierceStageNet (Without pretraining)} & \textbf{0.8504} & \textbf{0.8528} & \textbf{0.8502} & \textbf{0.8506} \\
		\bottomrule
	\end{tabular}
	\label{tab:comparison_all_models}
\end{table*}

\begin{table*}[t]
	\caption{Ablation Study on different Model Variants, Backbone Architectures, Pretraining, and Layer Freezing Strategies for Grapevine Leaf Disease Classification.}
	\centering
	\begin{tabular}{m{0.14\textwidth} m{0.10\textwidth} m{0.10\textwidth} m{0.10\textwidth} m{0.10\textwidth} m{0.10\textwidth} m{0.10\textwidth} m{0.08\textwidth}}
		\toprule
		\textbf{Model} & \textbf{Frozen Blocks} & \textbf{Pretraining} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Params (M)} \\
		\midrule
		EfficientNetB0          & 0 & ImageNet1K & 0.9501 & 0.9507 & 0.9501 & 0.9500 & 5.3 \\
		\textbf{PierceStageNet} & 0 & ImageNet1K & \textbf{0.9751} & \textbf{0.9754} & \textbf{0.9750} & \textbf{0.9749} & 6.1 \\
		EfficientNetB0          & 0 & None & 0.8329 & 0.8416 & 0.8329 & 0.8506 & 5.3 \\
		\textbf{PierceStageNet} & 0 & None & \textbf{0.8504} & \textbf{0.8528} & \textbf{0.8502} & \textbf{0.8506} & 6.1 \\
		EfficientNetB0          & 3 & ImageNet1K & 0.8120 & 0.8205 & 0.8121 & 0.8150 & 5.3 \\
		EfficientNetB0          & 4 & ImageNet1K & 0.8055 & 0.8100 & 0.8050 & 0.8075 & 5.3 \\
		EfficientNetB0          & 5 & ImageNet1K & 0.7982 & 0.8050 & 0.7985 & 0.8010 & 5.3 \\
		MobileNetV3 Small       & - & ImageNet1K & 0.8105 & 0.8150 & 0.8100 & 0.8125 & 2.9 \\
		ResNet50                & - & ImageNet1K & 0.8401 & 0.8450 & 0.8400 & 0.8425 & 23.5 \\
		\bottomrule
	\end{tabular}
	\label{tab:ablation}
\end{table*}

Figures~\ref{fig:eb_pretrained} and \ref{fig:eb_not_pretrained} provide the loss and accuracy curves for \textit{EfficientNet-B0} pretrained and not pretrained models.
The baseline EfficientNet-B0 exhibits less generalization compared to proposed \textit{PierceStageNet}. The \ac{se} block acts as a task specific attention adapter applied to the final feature representation. This enables domain aware channel recalibration under partial freezing. This results in validation accuracy improvement of ~4\% and a reduced training and validation gap.

The significant improvement of \textit{PierceStageNet} over the \textit{EfficientNet-B0} models is because of:
\begin{enumerate}
	\item Integration of external \ac{se} attention block to focus on disease specific area of leaf.
	\item Effective classification head tuned for multi-class disease stage classification with adjusted learning rate.
\end{enumerate}

These modifications show that carefully designed domain specific models can outperform general-purpose pretrained models in agricultural disease detection tasks.

\subsection{Ablation Study}
Our proposed architecture and the impact of the pre-training process were assessed thoroughly. Various ablation studies were conducted. Different variants of the EfficientNetB0 model with different frozen block sizes during the fine-tuning process: 0, 3, 4, and 5 frozen blocks, were assessed. Increasing the frozen blocks reduced the adaptability of the model. The best results were obtained with the initial 3 layers frozen, still lower than \textit{PierceStageNet}. However, there was a significant drop in the accuracy if 4 or 5 frozen blocks were frozen. In addition to the proposed model, other architectures including MobileNetV3-Small and the ResNet50 were also assessed and compared to the proposed model using the same training procedure. The results showed that the proposed model had higher accuracy compared to these two models also. All models and their results are given in Table~\ref{tab:ablation}.

\section{Limitations}
\label{sec:limitations}
While \textit{PierceStageNet} had strong performance in grapevine disease stage classification still several limitations must be acknowledged. Opportunities for improvement still exist.

\subsection{Limited Dataset}
One notable limitation is the dataset size and diversity. The model was trained and evaluated on a dataset of 4004 images which had 401 test samples with predefined disease stages. Although the reported metrics are high, the dataset may not fully contain the variability in Pierce Disease in different climates, lighting conditions, and devices. This can limit the generalizability of the model to different conditions in real vineyard scenarios.
\subsection{Confusion in Adjacent Disease Stages}
Additionally, the model exhibits challenges in distinguishing differences between adjacent disease stages, like in Stage 2 and Stage 3. It reflects the difficulty of progressive symptom recognition.
\subsection{Controlled Environment Experiment}
Environmental factors also pose a constraint. The current model was trained on images captured under a controlled environment. But on field deployment may bring varying lighting, occlusion by other leaves, dust, or may be overlapping plant structures.

\section{Future Directions}
\label{sec:future}
All of the above limitations can impact prediction accuracy. Although the dataset was balanced across disease stages during training; real world data is often skewed toward early or healthy stages. The performance of \textit{PierceStageNet} under such class imbalance conditions remains untested.
The limitations can be addressed by expanding the datasets to include more diverse environmental conditions. Adding multi-modal data like hyperspectral imaging or infrared can improve sensitivity to disease progression and reduce misclassifications between adjacent disease stages. For practical deployment, developing mobile or edge versions of \textit{PierceStageNet} would enable real-time disease detection in-field. Techniques such as focal loss, data augmentation or synthetic oversampling can help the model remain robust on imbalance datasets. Implementing incremental learning or domain adaptation strategies would allow the model to adapt over time to new disease variants or environmental conditions without full retraining.

In summary, while \textit{PierceStageNet} achieves high accuracy in experimental conditions, addressing these limitations will be essential for the practical deployment.


\section{Conclusion}
\label{sec:con}
In this paper, we developed an efficient model for multi-stage classification of grapevine leaves affected by Pierce's disease. The combination of a frozen \textit{EfficientNet-B0} backbone, \ac{se} attention module, and compact classification head gave a better accuracy while keeping computational costs low. Analysis of misclassifications showed that errors were primarily between adjacent stages and the model captures disease progression effectively enhanced with predictions \ac{gradcam} explainability. The model provides a practical solution for vineyard monitoring, supports early disease detection. Future work will focus on real-world deployment and extension to other grapevine diseases.

\vspace{12pt}
\color{black}

\bibliographystyle{IEEEtran}
\bibliography{references.bib}
\end{document}
